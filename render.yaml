services:
- type: cron
  name: insta-autopost-cron
  env: python
  schedule: "0 22 * * *"   # UTC22:00 → 日本時間07:00
  buildCommand: "pip install -r requirements.txt"
  startCommand: "python main_job.py"
  
